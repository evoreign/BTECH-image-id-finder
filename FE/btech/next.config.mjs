/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: [ "i.pinimg.com", "th.bing.com", "images.unsplash.com"]
    },
};

export default nextConfig;
